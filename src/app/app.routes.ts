import { Routes } from '@angular/router';
import { ProductReleasesComponent } from './components/product-releases/product-releases.component';

export const routes: Routes = [
  { path: '', component: ProductReleasesComponent },
];
